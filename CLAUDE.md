# Oni Quest Advisor

This project's AI guidance lives in [QWEN.md](./QWEN.md) — read it before working
here. It covers the stack, domain model, layered architecture, conventions, commands,
and the specs in `docs/`.

Per-layer rules live in the `CLAUDE.md` file inside each `src/` layer folder:
`routes/`, `lib/components/`, `lib/stores/`, `lib/domain/`, `lib/data`. Read the one
in a folder before adding files there.
